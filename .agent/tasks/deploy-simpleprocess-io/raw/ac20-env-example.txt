# Cloudflare API access (used by `wrangler` and DNS automation)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_ZONE_ID=

# Telegram Bot credentials for /api/contact form forwarder.
# Phase A leaves these unset — Pages Function deploys but stays inert.
# Restore in Phase B (token from @BotFather, chat id from a sample sendMessage).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
